# Copy to .env and adjust if needed. docker compose auto-loads .env from the project
# directory and uses it to fill ${LSC_CONFIG_DIR} in docker-compose-prod.yml.
#
# Absolute host path to the directory holding devices.json / wakeup.json / settings.json.
LSC_CONFIG_DIR=/volume2/dockerSSD/smart-lamps/config
